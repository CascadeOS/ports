diff --git coreutils-clean/lib/getlocalename_l-unsafe.c coreutils-workdir/lib/getlocalename_l-unsafe.c
index cac2f35..6f985d9 100644
--- coreutils-clean/lib/getlocalename_l-unsafe.c
+++ coreutils-workdir/lib/getlocalename_l-unsafe.c
@@ -658,6 +658,8 @@ getlocalename_l_unsafe (int category, locale_t locale)
       };
       const char *name = ((struct __locale_t *) locale)->mb_cur_max == 4 ? "C.UTF-8" : "C";
       return (struct string_with_storage) { name, STORAGE_INDEFINITE };
+#elif defined __cascade__
+      return (struct string_with_storage) { "C", STORAGE_INDEFINITE };
 #else
  #error "Please port gnulib getlocalename_l-unsafe.c to your platform! Report this to bug-gnulib."
 #endif
diff --git coreutils-clean/lib/mountlist.c coreutils-workdir/lib/mountlist.c
index 77ea8ce..b35c6e0 100644
--- coreutils-clean/lib/mountlist.c
+++ coreutils-workdir/lib/mountlist.c
@@ -1245,7 +1245,11 @@ read_file_system_list (bool need_fs_type)
 #endif
 
 #if MOUNTED_NOT_PORTED
-# error "Please port gnulib mountlist.c to your platform!"
+# if defined __cascade__
+  /* No mount-list backend yet; return an empty list. */
+# else
+#  error "Please port gnulib mountlist.c to your platform!"
+# endif
 #endif
 
   *mtail = NULL;
diff --git coreutils-clean/src/stat.c coreutils-workdir/src/stat.c
index e682bc6..9296fc8 100644
--- coreutils-clean/src/stat.c
+++ coreutils-workdir/src/stat.c
@@ -22,7 +22,8 @@
    ../m4/stat-prog.m4.  */
 #if ((STAT_STATVFS || STAT_STATVFS64)                                       \
      && (HAVE_STRUCT_STATVFS_F_BASETYPE || HAVE_STRUCT_STATVFS_F_FSTYPENAME \
-         || (! HAVE_STRUCT_STATFS_F_FSTYPENAME && HAVE_STRUCT_STATVFS_F_TYPE)))
+         || (! HAVE_STRUCT_STATFS_F_FSTYPENAME && HAVE_STRUCT_STATVFS_F_TYPE) \
+         || defined __cascade__))
 # define USE_STATVFS 1
 #else
 # define USE_STATVFS 0
@@ -255,6 +256,9 @@ human_fstype (STRUCT_STATVFS const *statfsbuf)
 {
 #ifdef STATXFS_FILE_SYSTEM_TYPE_MEMBER_NAME
   return statfsbuf->STATXFS_FILE_SYSTEM_TYPE_MEMBER_NAME;
+#elif defined __cascade__
+  (void) statfsbuf;
+  return "unknown";
 #else
   switch (statfsbuf->f_type)
     {
